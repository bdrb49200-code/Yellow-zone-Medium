# صناع البسمة — العملاء والإدارة

هذه نسخة تجهيز وربط وليست منشورة أو متصلة بقاعدة بيانات بعد.

- customer: ارفع محتوياته في جذر مستودع Yellow-zone-Medium.
- admin: ارفع محتوياته في جذر مستودع البيزك بعد تأكيد اسمه. لا تفترض أن Yellow-zone هو البيزك لأن عليه موقع سابق.
- setup/schema.sql: الجداول والصلاحيات وإنشاء الطلب. لم يُشغّل على حسابك.
- setup/seed.sql: جميع منتجات الأسعار وبيانات المراكز والصفحات كبداية.

## التجربة

شغّل `python3 -m http.server 8000` من مجلد الحزمة، ثم افتح http://localhost:8000/customer/ لتجربة العرض والسلة والمفضلة والبحث والترجمة.
افتح http://localhost:8000/admin/ واختر «معاينة لوحة الإدارة ببيانات تجريبية». هذه المعاينة في ذاكرة الصفحة فقط، ولا تعدّل موقع العملاء ولا تقبل بيانات طلبات حقيقية.

## الربط الفعلي

1. حدد مشروع Supabase الصحيح أولًا. يوجد مشروع باسم Kayan Entertainment Factory ومشروع آخر؛ لم نغيّر أيًّا منهما.
2. راجع schema.sql وشغله مرة واحدة على المشروع المختار، ثم شغّل seed.sql. الجداول الجديدة تبدأ بـ sm_ ولا تستبدل الجداول القديمة.
3. ضع رابط المشروع ومفتاحه العام publishable في config.js في الموقعين. لا تضع service_role أو secret.
4. فعّل Email Auth مع تأكيد البريد، واضبط Site URL وعناوين إعادة التوجيه على رابط موقع العملاء. إعداد SMTP مخصص مطلوب للبريد الإنتاجي المستقر.
5. أنشئ حساب المدير في Supabase Auth ثم أضف UUID الخاص به إلى sm_admins من SQL Editor. التسجيل العادي لا يمنح الإدارة.
6. اختبر حساب عميل وحساب مدير منفصلين: لا يجوز للعميل قراءة طلبات غيره أو تعديل المنتجات. اختبر إخفاء منتج ثم فتح الموقع من نافذة أخرى، وإرسال طلب والتحقق من ظهوره للمدير.
7. اضبط WhatsApp والشحن وتعليمات الدفع من الإدارة. لم نفترض رسومًا أو بيانات بنكية أو احتساب ضريبة قبل توضيح وضع الأسعار.

## ما تم تجهيزه

كتالوج 34 منتجًا، بحث مرادفات عربي/إنجليزي، سلة بكميات ومفضلة محفوظتان في المتصفح، نموذج حسابات، نموذج طلب عرض سعر، لوحات إدارة المنتجات والمراكز والصفحات والبنرات والإعدادات والطلبات ورفع الصور. الأسعار في الطلب يعاد احتسابها من قاعدة البيانات عند الإدخال، ولا يُعتمد على إجمالي المتصفح.

## حدود النسخة الحالية

نجح فحص بناء JavaScript واختبارات الكتالوج ومرادفات البحث ومعالجة النصوص والروابط. تعذر تنزيل متصفح الاختبار، لذلك لم يكتمل اختبار التفاعل والمقاسات بصريًا.
الربط والصلاحيات مكتوبة ولكن لم تُختبر على قاعدة بيانات فعلية. قبل الإطلاق يلزم اختبارها بالمشروع المختار وفحص Supabase advisors.
الصور الست المتوفرة توضيحية، ولا تمثل مواصفات فعلية معتمدة. المنتجات الأخرى بدون صورة بدل وضع صور خاطئة. ارفع صور المنتجات الأصلية من الإدارة. أسماء المراكز التجارية تحتفظ بالاسم العربي حتى اعتماد تهجئتها الإنجليزية.
اسم العلامة مكتوب بجوار الصورة الأصلية؛ لم يتم استخراج كتابة اللوجو كصورة شفافة بعد.
صفحة الطلب تطلب حسابًا مؤكدًا؛ طلب الزائر والنشرة البريدية واستعادة كلمة المرور لم يتم تنفيذها في هذه النسخة. لا يوجد دفع إلكتروني أو تحقق آلي من التحويل البنكي.
لا توجد ضمانات «تشفير 100%». كلمات المرور يديرها Supabase Auth ولا تُخزن في جداول التطبيق أو تعرض في لوحة الإدارة.

مراجع: https://supabase.com/docs/guides/auth/passwords و https://supabase.com/docs/guides/database/postgres/row-level-security
