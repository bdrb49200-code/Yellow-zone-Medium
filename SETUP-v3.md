# صناع البسمة — إصدار الصور

ملف Customers مخصص لمستودع Yellow-zone-Medium. ملف Admin مخصص لمستودع Yellow-zone-Basic. ملفات كل موقع موجودة مباشرة في جذر ملفه المضغوط.

## الصور
كل المنتجات الـ34 لها صور. أضيفت 32 صورة مولدة جديدة (31 للمنتجات وصورة قسم الخدمات)، مع الاحتفاظ بصور ثلاث منتجات موجودة. الصور توضيحية وليست صورًا معتمدة للبضاعة. صورة الديسرينة تصور مؤقت لعدم توفر مرجع. صورة قسم المراكز عامة ولا تمثل أي مركز مسمى. أوصاف التوليد محفوظة في IMAGE-PROMPTS.json.

## حسابات الإدارة
لا توجد حسابات مديرين افتراضية أو كلمات مرور جاهزة. إعداد الاتصال في config.js ما زال فارغًا في الموقعين. زر معاينة الإدارة يعمل بدون حساب، وتعديلاته محلية مؤقتة فقط.
يلزم تحديد مشروع Supabase الصحيح وبريد كل مدير قبل إنشاء الحسابات ومنح الصلاحيات. لا ترسل كلمات المرور في المحادثة ولا تضع مفاتيح سرية داخل هذه الملفات.

## تفعيل القاعدة لاحقًا
في المشروع المختار: راجع وشغّل setup/schema.sql ثم setup/seed.sql ثم setup/catalog-images-v3.sql. لا تشغل schema.sql مرة ثانية إن كان مطبقًا. ضع رابط المشروع والمفتاح العام فقط في config.js لكلا الموقعين. أنشئ حساب المدير في Auth ثم أضف UUID الخاص به في sm_admins. اختبر الصلاحيات والحسابات والطلبات قبل التشغيل الفعلي؛ لم يتم ذلك بعد.

## الرفع
ارفع ZIP في Codespaces للمستودع الصحيح وشغّل deploy.sh من الأرشيف باستخدام الأمر المرفق في المحادثة. يحتفظ الأمر بالنسخة القديمة في فرع احتياطي ويرفع التحديث إلى main. إذا ظهر HTTP403 عند ضبط Pages، فالرفع قد يكون نجح؛ افتح Settings > Pages واختر Deploy from a branch ثم main ثم / (root) واحفظ.

## الفحص
تم فحص بناء JavaScript ووجود صورة محلية لكل منتج وسلامة الأرشيف. هذه ليست شهادة باكتمال اختبار المتصفح أو Supabase. لا توجد بيانات دخول جديدة أو تغييرات على قاعدة البيانات في هذه النسخة.
